# Release Checklist

- update version in `pyproject.toml`
- update `CHANGELOG.md`
- run tests and compile checks
- review `SECURITY.md` and trust model docs
- verify LM Studio config examples still match current behavior
- verify privileged mode is not enabled in end-user examples
